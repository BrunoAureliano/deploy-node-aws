{{#each errors}}
    <div class="alert alert-danger">{{text}}</div>
{{else}}

{{/each}}

<h3>Edit Category</h3>
<div class="card">
    <div class="card-body">
        <form action="/admin/categories/edit" method="post">
            <input type="hidden" name="id" value="{{category._id}}">
            <label for="name">Name: </label>
                <input type="text" id="name" name="name" placeholder="Categories Name" class="form-control" value="{{category.name}}"> 
            <label for="slug">Slug: </label>
                <input type="text" id="slug" name="slug" placeholder="Categories Slug" class="form-control" value="{{category.slug}}"> 
                <br>
            <a href="/admin/categories/edit/{{_id}}">
                <button type="submit" class="btn btn-success">Edit Category</button>
            </a>
    </form>
    </div>
</div>